{
  "extends": "./tsconfig.build.json",
  "compilerOptions": {
    "baseUrl": "./packages",
    "paths": {
      "<%= monorepo.defaultPackage.packageName %>": ["<%= monorepo.defaultPackage.name %>/src"],
    }
  },
}
